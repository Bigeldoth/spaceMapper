import { type IconWrapperProps } from "./Icon";
/** Phosphor `rocket-launch-duotone` — activité « Piloter » du wizard. */
export declare function RocketLaunch({ size, ...rest }: IconWrapperProps): import("react").JSX.Element;
